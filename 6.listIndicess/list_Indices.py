Problem Statement :
===================    
    Given an unsorted integer array A and an integer value X, return the indices of the locations where X is found in A.
Input :
=======
    Unsorted integer array

Output :
========
    index Value - integer 
    
Code :
======    
def getIndex(array,indexFor):
    index_list = []
    iteratorValue = 0
    indexValue = 0
    while (i!=len(array):
        if (array[i] == indexFor):
            index_list.append(index)
        index += 1
        i += 1
    return index_list
